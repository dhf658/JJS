name: Telegram Casino Bot

on:
  push:
    branches: [ main ]
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        
    - name: Run Telegram Bot
      env:
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        ADMIN_USERNAMES: ${{ secrets.ADMIN_USERNAMES }}
      run: |
        echo "🤖 启动快三机器人..."
        timeout 290s python bot.py || echo "⏰ 运行完成"
        echo "✅ 本轮执行完成"
